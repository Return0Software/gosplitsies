# splitsies

## ui

### Setup

Install `node`.

To build and run on a fresh deploy.

```shell
cd ui
npm i
npm run dev
```

### Commands

```shell
npm run dev # Run the app
npm run lint # Lint code in ui/
```